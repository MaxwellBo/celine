When /version-bump is called, ensure that:

1. The appropriate changelog.xmls are updated
2. All the usage code uses new version numbers
3. deno.json files and package.json files use new version numbers 